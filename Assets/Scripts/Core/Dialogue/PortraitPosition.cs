namespace ProjectAstra.Core.Dialogue
{
    // Which side of the screen a speaker's portrait sits on for a line.
    // None hides the portrait entirely (narrator lines, or off-screen voices).
    public enum PortraitPosition
    {
        Left,
        Right,
        Center,
        None
    }
}
