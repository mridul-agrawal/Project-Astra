namespace ProjectAstra.Core.Units
{
    // Visual indicator of a unit's current Niyati state. Stored on save data
    // as the integer value — don't reorder; add new entries only at the end.
    public enum NiyatiSymbol
    {
        LotusFull,
        LotusHalf,
        LotusWithered
    }
}
