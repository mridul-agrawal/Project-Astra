namespace ProjectAstra.Core.Units
{
    // Whose side a unit is on. Stored on unit assets and save data as the
    // integer value — don't reorder; add new entries only at the end.
    public enum Faction
    {
        Player,
        Enemy,
        Allied
    }
}
