namespace ProjectAstra.Core.Audio
{
    // The mixer channels sound routes through. Each maps to a group and a volume slider.
    public enum AudioBus
    {
        Master,
        Music,
        Sfx,
        Ui,
        Ambient
    }
}
