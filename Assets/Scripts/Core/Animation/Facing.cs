namespace ProjectAstra.Core.Animation
{
    // Which way an on-map unit is facing. South points toward the camera — the
    // resting direction a unit shows when it stops moving.
    public enum Facing
    {
        South,
        North,
        East,
        West
    }
}
